# Compiler track (T8-T10) owns this file. Starts empty: the C++ DSL
# compiler (lexer/parser/validator/expander/resources/effect_registry/
# dsl_compiler — see ARCHITECTURE.md "The compiler") does not exist yet.
# T8 begins populating this list with lexer.{h,cpp}/parser.{h,cpp}/etc.
# Feeds into the noisemaker-qt static library target alongside
# NM_RUNTIME_SOURCES; see qt/CMakeLists.txt.
set(NM_COMPILER_SOURCES
)
